* Variable replacement.

* Expression evaluation for all formats.

* Mixins.

* Return expression values into the original format.  For example, if only pixels are used, return size to pixels.  Currently forces everything into points.

* Lazy-load all stuff that gets parsed more than once.

* Cross-browser fixes.

* Set up qUnit for template -> css comparisons.
